# Nima All Servers Setup Ansible Code

Step : [`superuser` Role](#superuser-role)
Step : [Configure Passwordless Sudo For User(s)](#configure-passwordless-sudo-for-users)
Step : [`preparing_server` Role](#delete-directory-sourceslistd)
Step : [Delete Directory `sources.list.d`](#delete-directory-sourceslistd)
Step : [Create Directory `sources.list.d`](#create-directory-sourceslistd)
Step : [Set Mirror `apt-repo.list`](#set-mirror-apt-repolist)
Step : [Update And Upgrade Apt Packages After Mirror Set](#update-and-upgrade-apt-packages-after-mirror-set)
Step : [Set Mirror `apt-repo.list`](#set-mirror-apt-repolist)
Step : [Delete Apt Proxy Config If Exist](#delete-apt-proxy-config-if-exist)
Step : [Set Nima Apt Proxy](#set-nima-apt-proxy)
Step : [Update And Upgrade Apt Packages After Mirror Set](#update-and-upgrade-apt-packages-after-mirror-set-1)
Step : [`sources.yaml` File](#sourcesyaml-file)
Step : [Copy `sources/debian` to `/etc/apt/sources.list.d/`](#copy-sourcesdebian-to-etcaptsourceslistd)
Step : [`resolved.conf` File](#resolvedconf-file)
Step : [Configure `resolved.conf` File](#configure-resolvedconf-file)
Step : [Configure `resolv.conf` File](#configure-resolvconf-file)
Step : [`packages.yaml` File](#packagesyaml-file)
Step : [Install Needed Tools](#install-needed-tools)
Step : [Install Needed Tools](#install-needed-tools)


Step : [Give Permission To SSH User](#give-permission-to-ssh-user)
Step : Edit Sources Files

## `superuser` Role

## Configure Passwordless Sudo For User(s)

## `preparing_server` Role

## Delete Directory `sources.list.d`

## Create Directory `sources.list.d`

## Set Mirror `apt-repo.list`

## Update And Upgrade Apt Packages After Mirror Set

## Delete Apt Proxy Config If Exist

## Set Nima Apt Proxy

## Update And Upgrade Apt Packages After Mirror Set

## `sources.yaml` File

## Copy `sources/debian` to `/etc/apt/sources.list.d/`

## `resolved.conf` File

## Configure `resolved.conf` File

## Configure `resolv.conf` File

## `packages.yaml` File

## Install Needed Tools


## Give Permission To SSH User

## For Constumizing Disk Size

Install `vagrant-disksize` plugin to manage that:

```bash
vagrant plugin install vagrant-disksize
```
# all_servers_setup
